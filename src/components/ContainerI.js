import styled from "styled-components";

export const ContainerI = styled.div`
  margin: 4rem 0;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  gap: 10rem;
  justify-content: center;
  align-items: center;
  color: var(--color-p);
  padding: 3rem 0;
`;
